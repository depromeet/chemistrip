import React, { Component } from 'react';
import {Login} from './container/auth';

class App extends Component {
    state = {
    }
    render() {
        return (
            <Login/>
        );
  }
}

export default App;
